Flask==3.0.0
gunicorn==21.2.0

# CORS (if you import/need it)
flask-cors

# Database (only if you use SQLAlchemy)
Flask-SQLAlchemy
SQLAlchemy

# Postgres driver (ONLY if you actually connect to Postgres on Render)
psycopg2-binary

# Twilio SMS (ONLY if your code imports twilio)
twilio

# .env support (ONLY if you call load_dotenv / use .env)
python-dotenv

# HTTP client (if you import requests)
requests
flask_login 
