# Core web framework
Flask==3.0.0
Flask-Cors
Flask-Login
Flask-SQLAlchemy

# ORM / database
SQLAlchemy
psycopg2-binary   # only needed if you connect to PostgreSQL on Render

# HTTP client
requests

# SMS / Email
twilio            # SMS (Twilio API)
# If you’re sending email via SMTP, Python already has smtplib built-in (no need to install)

# Environment variables support
python-dotenv

# Production server
gunicorn==21.2.0
